import React from 'react'
import Layout from "../components/layout"

export default function error() {
    return (
        <Layout>
            <h2>This is wrong on so many levels</h2>
        </Layout>
    )
}
